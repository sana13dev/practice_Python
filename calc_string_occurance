def char_with_freq(str):
    d = {}
    
    for i in str:
        if i in d:
            d[i] += 1
        else:
            d[i] = 1
    
    for k, v in d.items():
        print("{}{}".format(k,v), end = "")


Str = str(input("Enter the string: "))
char_with_freq(Str)
